<section class="card">

  <a href="/blog/{{id}}">
    <div class="title">
      <h2>{{title}} </h2>
      <p>Posted by: {{user.username}} on {{format_date createdAt}}</p>
    </div>    
    <!--header> Posted by: {{user.username}} on {{format_date createdAt}}</header-->
    <p>{{description}}</p>
    <!--img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}"-->
  </a>
</section>