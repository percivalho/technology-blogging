<section class="card">
  <a href="/myblog/{{id}}">
    <div class="title">
      <h2>{{title}}</h2>
      <p>{{format_date createdAt}}</p>
    </div>

    <!--header>{{title}}</header-->
  </a>
</section>
